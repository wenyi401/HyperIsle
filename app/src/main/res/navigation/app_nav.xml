<?xml version ="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/appNav"
    app:startDestination="@+id/mainNavFragment">
    <fragment
        android:id="@+id/mainNavFragment"
        android:name="art.luaj.hyperisle.ui.fragment.MainNavFragment"
        android:label="mainNavFragment">
        <action
            android:id="@+id/action_mainNavFragment_to_startFragment"
            app:destination="@id/startFragment" />
    </fragment>

    <fragment
        android:id="@+id/startFragment"
        android:name="art.luaj.hyperisle.ui.fragment.StartFragment"
        android:label="startFragment" />
</navigation>